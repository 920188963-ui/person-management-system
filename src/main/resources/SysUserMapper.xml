<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.companyusersystem.mapper.SysUserMapper">
    <select id="selectByUsername" resultType="org.example.companyusersystem.entity.SysUser">
        SELECT id, username, password, real_name AS realName, phone, email, role, status
        FROM sys_user
        WHERE username = #{username}
    </select>
    <insert id="insert" parameterType="org.example.companyusersystem.entity.SysUser">
        INSERT INTO sys_user(username, password, real_name, phone, email, role, status)
        VALUES (#{username}, #{password}, #{realName}, #{phone}, #{email}, #{role}, #{status})
    </insert>
</mapper>
